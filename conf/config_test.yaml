# 服务器配置
server:
  port: ":19001"
  name: "empyreanLens"

mongo:
  shadow: "_test"

  # 暂时先用lingo测试环境的数据库
  addr: "mongodb://svr_yanxubin:9fck23!g$i7k1%40eA@10.0.6.1:27017/empyrean_lens?authSource=admin&directConnection=true"
  databaseName: empyrean_lens
  maxPoolSize: 1000

# 日志配置
logger:
  log_path: './logs/system'
  log_level: "debug"
