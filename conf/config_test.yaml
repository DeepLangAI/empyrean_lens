# 服务器配置
server:
  port: ":8080"
  name: "empyreanLens"

# 日志配置
logger:
  log_path: './logs/system'
  log_level: "debug"

metrics:
  log_store: "qa-metrics"
  metric_prefix: "empyrean-lens"

external_secret:
  deeplang_sls_fc_secret:
    base_url: "http://mcp-sls-lingowhle-fc-qa-kisjjtktbb.cn-zhangjiakou.fcapp.run"
    token: "REDACTED"
notice:
  lingowhale_stability_webhook: "https://open.feishu.cn/open-apis/bot/v2/hook/00000000-0000-0000-0000-000000000000"

feishu:
  app_id: "cli_a668c41a0f64d013"       # 飞书开发者后台 App ID（凭证与基础信息）
  app_secret: "REDACTED"   # 飞书开发者后台 App Secret
  redirect_url: "https://qa-empyrean-lens.lingowhale.com/auth/callback"  # 需在安全设置中添加
  jwt_secret: "dev-secret-please-replace-with-32chars"
